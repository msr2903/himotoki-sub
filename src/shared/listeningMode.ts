/**
 * Listening practice mode: blur the subtitle text so the learner listens first, revealing the line on
 * hover or with the H "peek" hotkey. Persisted so it survives reloads; the transient peek state lives
 * in the settings model.
 */
export const LISTENING_MODE_SETTING = "listeningMode";
export const DEFAULT_LISTENING_MODE = false;
